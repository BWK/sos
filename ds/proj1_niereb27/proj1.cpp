// Rebar Niemi
// SOS 2013
// Data Structures Project 1
#include <iostream>
#include <vector>

using namespace std;

// both input and insertJob will be scheduler class methods
void input() {
  //gets user input, calls insert job
  insertJob();
}

bool insertJob(job_description, n_procs, n_ticks) {
  //will return false if job creation fails
}

int main () {
  while (true) {
    scheduler.input();
    scheduler.tick()
  }
  
  return 0;
}

